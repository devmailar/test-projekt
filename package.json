{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild game.js --bundle > build.js"
  },
  "dependencies": {
    "esbuild": "^0.15.3",
    "kaboom": "^2000.2.9"
  }
}
